{
  "NLog": {
    "rules": [
      {
        "logger": "System.Net.Http.*",
        "finalMinLevel": "Info"
      },
      {
        "logger": "*",
        "minLevel": "Trace",
        "writeTo": "dev-console"
      }
    ]
  }
}
