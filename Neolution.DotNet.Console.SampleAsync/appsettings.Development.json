{
  "NLog": {
    "rules": [
      {
        "logger": "System.Net.Http.*",
        "finalMinLevel": "Warn"
      },
      {
        "logger": "*",
        "minLevel": "Trace",
        "writeTo": "console"
      }
    ]
  }
}
